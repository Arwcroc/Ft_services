apiVersion: v1
kind: Service
metadata:
  name: wordpress
  labels:
    app: ft_services
spec:
  type: LoadBalancer
  selector:
    run: wordpress
  ports:
    - name: wordpress
      protocol: TCP
      port: 5050
      nodePort: 30050