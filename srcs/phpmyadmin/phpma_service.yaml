apiVersion: v1
kind: Service
metadata:
  name: phpmyadmin
  labels:
    app: ft_services
spec:
  type: LoadBalancer
  selector:
    run: phpmyadmin
  ports:
    - protocol: TCP
      port: 5000
      nodePort: 30000
